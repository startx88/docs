/**
 * Location: this object contain the information about the crrent URL
 * this location object is part of window object and is accessed through the window.location property.
 *
 * Properties:
 * a.   hash - return the hash part of the url (ex: #info)
 * b.   host - return the hostname and port of the url (ex: "127.0.0.1:5500")
 * c.   hostname -
 * d.   port - return the port of the url (ex: 5000)
 * e.   href - return the whole url (ex: " "http://127.0.0.1:5500/domApi/index.html"")
 * f.   origin - return the protocol, hostname and port number of a URL (ex: "http://127.0.0.1:5500")
 * g.   pathname
 * h.   protocol - return the protocol (ex: 'http:')
 * i.   search -
 *
 * Methods:
 * a.   assign() - Loads a new document
 * b.   reload() - Reloads the current document
 * c.   replace() - Replaces the current document with a new one
 */