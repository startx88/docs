/******
 * Indexes: Retrieving data efficently
 * 1. Indexes support the efficient execution of queries in MongoDB.
 * 2. Without indexes, MongoDB must scan every document in a collection (a collection scan) to find the documents that match each query.
 * 3. These collection scans are slow and can negatively affect the performance of your application.
 *
 *
 * note:
 * But don't use many indexes. using more indexes will result in performance issues.
 *
 * What are indexes:
 * Indexes can speed up update and delete operations.
 *
 *
 * Different types of indexes:
 *
 *
 * Using and optimized indexes
 *
 *
 *
 *
 * */