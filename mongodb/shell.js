/******
 * Mongodb Shell:
 *
 *
 *
 * Setting dbpath or logpath:
 *
 * set paths:
 * mongod --dbpath /users/pradeep/mongodb/db --logpath /users/pradeep/mongodb/logs
 *
 *
 * run server in background:
 *
 * mongod --fork --logpath /users/pradeep/mongodb/logs/log.log // in mac
 *
 * cmd: net start MongoDB or db.shutdownServer()
 *
 * */