/**
 * String:
 * string are usefull to holding data that can be represented int the text form.
 *
 * static properties:
 * 1. length
 *
 * Instance method:
 * 1. chartAt(index:number) => return the character at specified index.
 * 2. charCodeAt(index:number) => return code of character at specified location.
 * 3. concat(...string:string[]) => contact two or more string and return new string.
 * 4. indexOf(searchString,pos?:number) => Return the position of first occurs.
 * 5. lastIndexOf(searchString: string, position?: number) => Returns the last occurrence of a substring in the string
 * 6. localeCompare(that: string) => Determines whether two strings are equivalent in the current locale
 * 7. match(regexp: string | RegExp) => Matches a string with a regular expression, and returns an array containing the results of that searchString
 * 8. replace(searchValue: string | RegExp, replaceValue: string) => Replaces text in a string, using a regular expression or search string
 * 9. search(regexp: string | RegExp) => Finds the first substring match in a regular expression search.
 * 10.slice(start?: number, end?: number) => Returns a section of a string.
 * 11.includes(searchString, position) => Performs a case-sensitive search and return true/false
 * 12.
 */