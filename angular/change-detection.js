/**
 * Angular change detecton strategy:
 *
 * What is change detection:
 * Change Detection means updating the view (DOM) when the data has changed
 *
 *
 * Change detection is done in two types;
 * 1. Default :
 *    this is default change decation strategy in angular when you create any new component.
 *    component re-render every time when detect any changes.
 * 2. OnPush
 */