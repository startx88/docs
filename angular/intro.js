/**
 * What is angular:
 * Angular is a javascript framework for building reactive SPA.
 *
 * How to create an application in angular.
 * Step: 1 first install @angular/clien
 *       2. ng new project_name
 *       3. Open in editor
 *       4. ng serve :- for running project
 *
 * How an angular app loaded and started: app
 * 1.
 *
 * Components:
 * are a key feature of angular.
 *
 *
 * Databinding in angular:
 * 1. String interpolation {{}}
 * 2. Property building [innerText]="name"
 * 3. Event Binding (name)="name"
 *
 *
 * Lifecycles:
 * 1. ngOnChange: called after a bound input property change.
 * 2. ngOnInit: called once the component initialize.
 * 3. ngDoCheck: Called during every change detection run.
 * 4. ngAfterContentInit: Called after content (ng-content) has been projected into view.
 * 5. ngAfterContentChecked:  Called every time the projected content has been checked
 * 6. ngAfterViewInit: Called after the component's view has been initialized.
 * 7. ngAfterViewCheck:  Called every time view has been checked.
 * 8. ngOnDestroy: Called once the component is about to be destroyed.
 *
 *
 *
 * Directives:
 *
 *
 * Create a basic attribute directive:
 *
 */