/*********
 * Waht are the template boundary or template variable scope?:
 * Ans: you can define template variable within its sarounding template, but *ngIf, *ngFor and ng-template provide template boundary,
 * you can not access template variables outside of them.
 */