/**
 * Es Lint:
 *
 * 1. Es Lint uses Espree for javascript parsing.
 * 2. Es Lint uses an AST to evalute patterns in code.
 * 3. Es Lint is comletely pluggable, every single rule is a plugin and yu can add more at runtime.
 *
 *
 * Install globally:
 * npm install -g eslint
 *
 * Locally install:
 * npm install eslint -D
 */