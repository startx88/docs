/**
 * Vue Component:
 * 1. Components are reusable vue instances.
 * 2. They accepts the same option as new Vue.
 * 3. The only exceptions are few root-specific element, like: el
 *
 * 4. Data must be a function
 */