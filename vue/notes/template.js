/**
 * Vue js use an html base template syntax that allow you to declarative binding
 * and rendering DOM to the underlying component instance's data.
 *
 * All vuejs template are valid html that can be parsed by spec-compilant browsers and html parsers
 *
 * Vue compile the template into vertual dom render function combined with the reactive stystem.
 *
 * # interpolation
 * 1. The most basic form of data binding in html use "mustache syntax"
 *    Ex: {{message}}
 *
 * You can also perform on time compilation using "v-once" directive.
 *
 */