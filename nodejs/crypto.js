/**
 * What is crypto: cryto module provide the cryptographic functionality that include a set of wrappers for OpenSSL hash,HMAC, cipher, decipher, sign, and verify functions.
 * HMAC: Hash-based massage authentication code
 * Cipher: in cryptography, a cipher (or cypher) is an algorithm for performing encryption or decryption—a series of well-defined steps that can be followed as a procedure.
 */
const crypto = require("crypto");
console.log(crypto);
