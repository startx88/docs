/**
 * What is socket:
 *
 */

/**
 * Packet: small strem of data
 * there are basically 5 layer in every packet.
 *
 * Application Layer: http,ssh (in linux macine), smtp, ftp
 * Transport Layer: UDP/TCP
 * Network Layer: IP
 * Link Layer: wi-fi, either net
 * Physicla Layer: cables (actuall physical cable that connected)
 */

/**
 * There are two types of transfer protocol
 * 1. TCP:
 * 2. UDP:
 *     1. light weight
 *     2. only 8 byte header
 *     3. connection less
 *     4. Consistency
 *     5. Fast
 */